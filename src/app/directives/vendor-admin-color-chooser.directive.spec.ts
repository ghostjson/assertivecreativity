import { VendorAdminColorChooserDirective } from './vendor-admin-color-chooser.directive';

describe('VendorAdminColorChooserDirective', () => {
  it('should create an instance', () => {
    const directive = new VendorAdminColorChooserDirective();
    expect(directive).toBeTruthy();
  });
});
